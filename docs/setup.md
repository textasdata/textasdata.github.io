# Jekyll Startup (on Mac)

## Install Jekyll
1. Install a new version of ruby (via homebrew)
```
brew isntall ruby
```
2. Install Jekyll
```
gem install jekyll
```
  - you may have to install dependencies
    ```
gem install jekyll-sitemap
gem install jekyll-feed

    ```
